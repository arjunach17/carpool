package com.carpooling.entity.enums;

public enum BookingStatus {
    REQUESTED,      // Booking created, awaiting confirmation
    CONFIRMED,      // Booking confirmed, passenger on ride
    CANCELLED       // Booking cancelled
}
